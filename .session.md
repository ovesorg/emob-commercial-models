# emob-commercial-models Session Log

## 2026-01-13 | Dr. H Wang

**Task:** Service Products SO Logic - Commercial Guide Update

**Context:** Updated commercial/user-facing documentation to reflect new Service-only SO workflow from `odoo-migration` technical architecture.

**Files Modified:**
- `docs/3-product-bundle-library/4-odoo-service-bundle.md` (added Service-only SO workflow)
- `mkdocs.yml` (editorial metadata, navigation updates)

**Key Changes:**

1. **Added Service-only SO Workflow (Option B):**
   - Step 1: Find original Bundle SO for customer
   - Step 2: Create Service-only SO with `source_so_id` linkage
   - Step 3: Add service products (with validation rules)

2. **Temporal Eligibility Table:**
   - Added user-facing table showing service time constraints
   - Example: E3Pro Extended Warranty (30 days max after purchase)
   - Example: E3Pro Swap Renewal (no limit, requires prior swap service)

3. **Commercial Constraints Documentation:**
   - Ownership rule: Must be same customer as original purchase
   - Compatibility rule: Services must match asset type
   - Prerequisite rule: Some services require prior services

4. **Site-wide Editorial:**
   - Configured in `mkdocs.yml` extra block
   - Edition: "Service Products SO Logic"
   - Editor: Dr. H Wang

**Cross-Repo Coordination:**
- Aligned with technical implementation in `odoo-migration` repo
- Technical reference: `odoo-migration/docs/assets-serial-numbers/service-bundles-intents.md`

**Build & Deploy:**
- Ran `mkdocs-oves-krr --port 8002`
- Site served at http://127.0.0.1:8002

**Git Status:**
- Committed: "Service Products SO Logic - Commercial Guide Update"
- Pushed to GitHub remote: main branch

**Next Steps:**
- Create sales training materials based on new workflow
- Add screenshots/mockups for UI guidance (when Odoo implementation available)
- Consider adding example scenarios for common service purchase cases

**Blockers:** None

**References:**
- Technical architecture: `odoo-migration` repo (service-bundles-intents.md)
- Port assignments: See `d:/github/PORT-CONVENTIONS.md`

## 2026-03-18 | Codex

**Task:** Local global-search pilot integration for native MkDocs search

**Context:** Used `emob-commercial-models` as the first child-site proving ground for cross-site global search while preserving repo-native MkDocs docs.

**Files Modified:**
- `docs/javascripts/global-doc-home.js`
- `docs/javascripts/global-search-service.js`
- `mkdocs.yml`

**Key Changes:**

1. **Document Hub Return Path**
   - Updated the top-left MkDocs logo behavior to act as `Back to Document Hub`
   - Local target resolves to `http://127.0.0.1:8099/internal/consolidated-docs/`
   - Cloud target remains `https://docs.omnivoltaic.com/internal/consolidated-docs/`

2. **Native Search Bar Hook**
   - Wired the existing Material search modal to call the local `docs-search` backend
   - Local API target is `http://127.0.0.1:8105`
   - Results render into the native MkDocs search result list

3. **Validation**
   - `mkdocs build --clean` passed
   - `mkdocs-oves-krr --port 8018` completed
   - Rebuilt site confirmed serving `global-search-service.js`

**Current Outcome:**
- Local search bar can now participate in global search against the backend
- Result quality/length still needs refinement before broader rollout

**Blockers / Notes:**
- One PlantUML render timed out during KRR for `example-c4-container.puml`, but the MkDocs site build still succeeded
- This repo is currently a pilot child-site for the global search integration
## 2026-03-18 | Header Baseline And Semantic Registry
- Adopted the latest MkDocs header baseline:
  - `Doc Hub` logo label
  - dedicated `Global` button beside native local search
  - local-search query carryover into hub global search
- Added semantic registry generation hook for hub search participation.
- Validation:
  - local serve/KRR passed on port `8018`

## 2026-03-19 | Template Resync And Stable Global Bridge
- Re-synced the repo to the current MkDocs template baseline and added `.docs-template-info.json`.
- Standardized the human-facing site title to `eMob Commercial Models`.
- Updated the hub/global bridge so the local header search can jump to the hub custom global-search page without touching native MkDocs search semantics.
- Switched carry-over from `?q=` to `#oves-global-query=...`.
- Key files touched:
  - `mkdocs.yml`
  - `docs/javascripts/global-doc-home.js`
  - `hooks/build_semantic_registry.py`
  - `.docs-template-info.json`
  - `.mkdocs-migration.json`
- Validation:
  - `python -m mkdocs build --clean` passed
  - local site on `http://127.0.0.1:8018/` coordinated correctly with the local hub on `http://127.0.0.1:8017/`

## 2026-03-20 | Template Resync To Clean Curation Status
- Re-synced the repo to the latest `mkdocs-template` baseline using `mkdocs-oves-migrate .`.
- Added the missing hub/search bridge metadata expected by curation:
  - `.docs-template-info.json` now includes `global_search_bridge_version: 1`
  - `.docs-template-info.json` now includes `hub_integration_enabled: true`
  - `mkdocs.yml` now includes `extra.oves_hub_integration.enabled: true`
- Refreshed shared template assets and partials from the package baseline.
- Validation:
  - `consolidated-docs` curation inventory now reports this repo as `pass`
  - local review suite verified HTTP `200` on `http://127.0.0.1:8018/`

## 2026-03-21 | Self-Contained Build Hooks For Cloud Deployment
- Removed the deploy-time dependency on `mkdocs-oves-template`.
- Replaced package-import hook stubs with local build-critical hook implementations so the repo can build independently in CI/CD.
- Removed inactive i18n package dependency from `requirements.txt`.
- Key files touched:
  - `hooks/update_date.py`
  - `hooks/build_semantic_registry.py`
  - `requirements.txt`
- Validation:
  - `python -m mkdocs build` passed
  - KRR/local review verified HTTP `200` on `http://127.0.0.1:8018/`
  - PlantUML rendering attempted during KRR but local Java is older than the bundled `plantuml.jar`; site build still succeeded

## 2026-03-22 | Template Normalization And Review Wrap
- Applied the idempotent template normalization baseline to keep the repo independently buildable and aligned with the current single-language standard.
- Re-synced local self-contained hooks, template marker, and remaining override drift while preserving the current content shape.
- Key files touched:
  - `mkdocs.yml`
  - `hooks/update_date.py`
  - `hooks/build_semantic_registry.py`
  - `overrides/stylesheets/extra.css`
  - `.docs-template-info.json`
- Validation:
  - `mkdocs-oves-krr --port 8018 --project-root D:\github\emob-commercial-models` passed
  - local review verified HTTP `200` on `http://127.0.0.1:8018/`
  - rendered footer shows exactly one editorial trail with `Build`
  - backlog: PlantUML rendering still fails locally because the Java runtime is older than the bundled `plantuml.jar`, but static site build succeeds

## 2026-03-22 | Post-Normalization Wrap
- Re-applied the current idempotent normalization baseline from
  `mkdocs-template` and synchronized the child-site CSS assets used by the
  shared hub/search bridge.
- Key files touched:
  - `docs/stylesheets/extra.css`
  - `overrides/stylesheets/extra.css`
  - `.session.md`
- Validation:
  - `python -m mkdocs build --clean --strict` passed
